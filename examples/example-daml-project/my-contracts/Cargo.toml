[package]
name = "my-contracts"
version = "0.1.0"
edition = "2024"

[dependencies]
canton = { workspace = true, features = ["full"] }

[build-dependencies]
canton = { workspace = true, features = ["codegen", "dpm-build"] }
