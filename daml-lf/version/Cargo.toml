[package]
name = "daml-lf-version"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { workspace = true, features = ["derive"], optional = true }
